/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "example.com",
      "another-domain.com",
      "images.unsplash.com",
      "www.2playbook.com",
    ],
  },
};

export default nextConfig;
