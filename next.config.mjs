/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "example.com",
      "another-domain.com",
      "images.unsplash.com",
      "www.2playbook.com",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
